apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-ingress
spec:
  rules:
  - host: http://ec2-15-161-175-169.eu-south-1.compute.amazonaws.com/
    http:
      paths:
      - path: /
        backend:
          service:
            name: todosmanagerservice
            port:
              number: 80
